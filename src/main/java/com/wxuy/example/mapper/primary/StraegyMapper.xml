<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wxuy.example.mapper.primary.StraegyMapper">

    <select id="findUserA" parameterType="String" resultType="StraegyUser">
                select * from ResourceA where id = #{id};
    </select>

    <select id="findUserB" parameterType="String" resultType="StraegyUser">
                select * from ResourceB where id = #{id};
    </select>

    <select id="findUserC" parameterType="String" resultType="StraegyUser">
                select * from ResourceC where id = #{id};
    </select>
</mapper>